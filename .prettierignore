node_modules
dist
bin
build
# Tooling that produces build output, not build output. See the matching note in .gitignore.
!scripts/build
.docusaurus
coverage
test-results
playwright-report
.env
*.log
bun.lock

# Committed verbatim from a supplied source. Prettier renumbers markdown ordered lists, which
# silently rewrites the deliberately out-of-sequence user story 23 into a duplicate of 15 — the
# exact edit the file's own provenance comment forbids.
docs/requirements.md

# Generated by scripts/build/build-publisher.ts: a vendored Decap bundle and a minified shim.
apps/docs/static/publisher/*.js

# Exported verbatim from Claude Design and re-synced wholesale, so formatting it here would be
# undone by the next export. The CSS also column-aligns its token comments, which Prettier
# collapses — the alignment is what makes the palette scannable.
.claude/skills/

# Every documentation page carries an `owner` in frontmatter; this file is what turns that into
# an actual review request. Keep the two consistent — a page whose owner has no CODEOWNERS entry
# routes to nobody.

*                       @m-howard

# The corpus. Once the authoring gateway lands, these entries are the enforcement point for
# review routing (requirements.md R8), so add a team per documentation area as they appear.
/docs/                  @m-howard
/docs/adr/              @m-howard

# Changes here alter what a deployed task, or GitHub Actions, can reach.
/infra/pulumi/src/components/gateway-service.ts    @m-howard
/infra/pulumi/src/components/docs-knowledge-base.ts @m-howard
/infra/pulumi/src/components/publish-pipeline.ts   @m-howard
/infra/pulumi/src/components/cms-credential.ts     @m-howard

# Who may push to the corpus, and under what review.
/infra/pulumi/src/components/corpus-repository.ts  @m-howard

# Path handling: both files resolve untrusted input to a location.
/apps/gateway/src/kb/key-policy.ts    @m-howard
/apps/gateway/src/routes/site.ts      @m-howard

# Supply chain and CI.
/.github/               @m-howard
/package.json           @m-howard
/bun.lock               @m-howard

# Every documentation page carries an `owner` in frontmatter; this file is what turns that into
# an actual review request. Keep the two consistent — a page whose owner has no CODEOWNERS entry
# routes to nobody.

*                       @m-howard

# The corpus. These entries are the enforcement point for review routing (requirements.md R8) —
# the gateway opens a pull request and branch protection requires a code-owner approval on it, so
# add a team per documentation area as they appear.
/docs/                  @m-howard
/docs/adr/              @m-howard

# Changes here alter what a deployed task, or GitHub Actions, can reach.
/infra/pulumi/src/components/gateway-service.ts    @m-howard
/infra/pulumi/src/components/docs-knowledge-base.ts @m-howard
/infra/pulumi/src/components/publish-pipeline.ts   @m-howard
/infra/pulumi/src/components/cms-credential.ts     @m-howard

# Reader questions and who can read them. The gateway may only write to this table and the report
# may only read from it; a change loosening either is the one worth catching in review. See
# docs/adr/0015-recording-documentation-gaps.md.
/infra/pulumi/src/components/gap-feedback-table.ts   @m-howard
/infra/pulumi/src/components/gap-report-pipeline.ts  @m-howard
/apps/gateway/src/feedback/                          @m-howard
/apps/gateway/src/routes/feedback.ts                 @m-howard
/scripts/docs/gap-report.ts                          @m-howard

# Who may push to the corpus, and under what review.
/infra/pulumi/src/components/corpus-repository.ts  @m-howard

# Path handling: both files resolve untrusted input to a location.
/apps/gateway/src/kb/key-policy.ts    @m-howard
/apps/gateway/src/routes/site.ts      @m-howard

# The authoring gateway. Everything here decides who may write what, with a credential no author
# holds — a widening change is the one worth catching in review rather than in an audit log.
/apps/gateway/src/auth/                @m-howard
/apps/gateway/src/git/                 @m-howard
/apps/gateway/src/cms/                 @m-howard
/apps/gateway/src/routes/cms.ts        @m-howard
/apps/gateway/src/audit.ts             @m-howard

# Supply chain and CI.
/.github/               @m-howard
/package.json           @m-howard
/bun.lock               @m-howard

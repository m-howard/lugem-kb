# GEMINI.md

@AGENTS.md

**All project rules live in [`AGENTS.md`](AGENTS.md)** — the single source of truth for every AI
assistant (workspace layout, file placement and repo-root hygiene, the package manager rule,
testing and the coverage gate, coding style, documentation rules, git workflow). Read it in full;
do not re-add project rules here.

Nothing in this project is Gemini-specific. If a Gemini-only operational note ever becomes
necessary, it belongs below this line — never a duplicate of a rule that already lives in
`AGENTS.md`, which would drift.
